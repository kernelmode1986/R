一、随机森林random_forest
1.500棵树的random_forest用于分类，返回的预测结果y的值域为[0,0.002,0.004,...,1]。原因是500颗树进行投票，所以结果是{i/500|i=0..500}
2.importance


二、ks.test


min=
(max_pro(1,i-1),a[i]-min),max_pro(1,i-1)+diff


买，再卖。


max(1,i)：至少交易一次，现在的价值。
max(1,i-1),max_pro(1,i-1)+diff,



global[i,j]=max(global[i-1,j],local[i,j])

local[i,j]=global[i-1,j-1],

